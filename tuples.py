#tuples is enclosed in a small bracket()
#Tuples and list are similar
tup=(1,3,76,8,"hi")#this is a tuple which is immutable(can not be edited )
print(tup[-1])
#tup.append("hello") In the list we can add something by using append but in tuples we can't
t=[12,3,4,5,6,6]
print(t[0],t[3],t[2])